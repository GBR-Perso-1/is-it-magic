# wip/

Work-in-progress artefacts that are not production-ready and should not be loaded by the plugin.

Skills and agents here are excluded from `skills/` and `agents/` so they are not auto-discovered by Claude Code.

## Contents

| Path | Status | Notes |
|---|---|---|
| `skills/project-inject-dev-settings/` | WIP | Dev credential injection — needs `it--dev-settings` infra to be set up first |
| `skills/project-migrate-dev-settings/` | WIP | Secret scanning + history sanitisation — depends on inject skill being stable |

Move artefacts back to `skills/` or `agents/` once they are ready.
